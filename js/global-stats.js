// Система статистики отключена
// Файл оставлен для совместимости с другими скриптами

window.GlobalStats = {
    recordDownload: (section) => {
        // Функция оставлена пустой для совместимости
        console.log('Скачивание зафиксировано для:', section);
    }
};